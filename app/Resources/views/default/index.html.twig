{% extends 'base.html.twig' %}

{% block body %}
    {% for msg in app.session.flashBag.get('info') %}
        <div class="alert alert-success" role="alert" id="info">
            {{ msg }}
        </div>
    {% endfor %}

    {% for msg in app.session.flashBag.get('errors') %}
        <div class="alert alert-danger" role="alert">
            {{ msg }}
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    {% endfor %}

    <div class="container body-content">
        <div class="row">
            {% for jewelleries in jewellery %}
                <div class="col-md-6">
                    <div>

{#                        <header>#}
{#                            <h2>{{ jewellery.name }}</h2>#}
{#                        </header>#}

{#                        {% if jewellery.image %}#}
{#                            <a href="{{ path('jewellery_view', {id: jewellery.id}) }}">#}
{#                                <img src="{{ asset('uploads/images/jewellerys/' ~ jewellery.image) }}" width="400px" height="200px">#}
{#                            </a>#}
{#                        {% endif %}#}

{#                        <p>#}
{#                            {{ jewellery.summary }}#}
{#                        </p>#}

{#                        <small>Views: {{ jewellery.getViewCount() }}</small><br />#}
{#                        <small>Added On: {{ jewellery.getDateAdded()|date("F jS \\a\\t g:ia") }}</small>#}
{#                        <small class="author">#}
{#                            {{ jewellery.author.fullName }}#}
{#                        </small>#}

{#                        <footer>#}
{#                            <div class="pull-right">#}
{#                                <a class="btn btn-default btn-xs"#}
{#                                   href="{{ path('jewellery_view', {id: jewellery.id}) }}">Read more &raquo;</a>#}
{#                            </div>#}
{#                        </footer>#}
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
